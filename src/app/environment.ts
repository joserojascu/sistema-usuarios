export const environment = {
    production: false, // Variable que indica si estamos en entorno de producción o no
    apiUrl: 'http://localhost:3000' // URL de la API, puede variar según el entorno
  };